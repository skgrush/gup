import { DraggableHeaderDirective } from './draggable-header.directive';

describe('DraggableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DraggableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
